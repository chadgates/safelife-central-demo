# Copy to deploy/.env next to docker-compose.yml on the instance.

# The image built by GitHub Actions. Lowercase owner and repo.
IMAGE=ghcr.io/chadgates/safelife-central-demo:latest

# Leave as :80 until DNS points at the instance, then switch to the hostname
# and Caddy will get a certificate automatically.
SITE_ADDRESS=:80
